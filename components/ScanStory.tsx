"use client";
import { motion, useScroll, useSpring, useTransform } from "framer-motion";
import { useRef } from "react";
export default function ScanStory(){const ref=useRef<HTMLElement>(null);const {scrollYProgress}=useScroll({target:ref,offset:["start start","end end"]});const p=useSpring(scrollYProgress,{stiffness:75,damping:26});const clip=useTransform(p,[.05,.92],["inset(0 100% 0 0)","inset(0 0% 0 0)"]);const x=useTransform(p,[.05,.92],["0%","100%"]);return <section ref={ref} className="scan-wrap"><div className="scan-sticky shell"><div className="scan-copy"><p className="eyebrow">SCROLL 02 / DEL PLANO A LA REALIDAD</p><h2>La ejecución valida cada línea del proyecto.</h2><p>Desplaza el escáner: la retícula técnica se convierte en volumen, huecos, material y fachada.</p></div><div className="scan-scene"><div className="scan-blue"><div className="scan-grid"/><div className="wire-building"><i/><i/><i/><i/><i/></div><span>IFC / MODEL / REV03</span></div><motion.div className="scan-built" style={{clipPath:clip}}><div className="built-sky"/><div className="built-building"><i/><i/><i/><i/><i/><i/></div><div className="built-ground"/></motion.div><motion.div className="scanner" style={{left:x}}><b/></motion.div></div><div className="scan-meta"><span>MODEL</span><span>COORDINATION</span><span>EXECUTION</span><span>00—100%</span></div></div></section>}
